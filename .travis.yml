language: go

services:
  - mongodb

addons:
  apt:
    sources:
      - mongodb-3.0-precise
    packages:
      - mongodb-org-server

go:
  - 1.6

env:
  global:
    - GO=go
    - JUJU_MONGOD=/usr/bin/mongod
